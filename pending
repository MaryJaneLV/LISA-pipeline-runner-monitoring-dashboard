- Fix suspend/resume status updates
- Surface error messages
- Rethink storage per user / path definitios
- Check about python modules as input (last in the list of priorities)
- Check if we can delete templates/workflows from the DB using events (so that they are deleted from the db if they are deleted in argo)
- Check how can we fix
  - Name stored in argo different than the one we use (for templates). This causes that sometimes when we want create a workflow argo send back that it can't find the template
  - Unique names for templates - if someone else creates the same template name we can't upload one


  - Add unit tests
  - Add integration tests
  - Add patterns
    - Resicilience
    - Observability
    - Security Implementation