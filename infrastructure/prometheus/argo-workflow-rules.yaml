# apiVersion: monitoring.coreos.com/v1
# kind: PrometheusRule
# metadata:
#   name: argo-workflow-cpu-rules
#   namespace: scientific-workflow
#   labels:
#     release: prometheus # Must match your Prometheus Helm release name
# spec:
#   groups:
#     - name: argo-workflow-cpu
#       interval: 30s
#       rules:
#         - record: argo:workflow_cpu_usage:rate5m
#           expr: |
#             sum by (workflow) (
#               rate(container_cpu_usage_seconds_total{namespace="scientific-workflow"}[5m])
#               * on(pod) group_left(workflow)
#               kube_pod_labels{namespace="scientific-workflow", label_workflows_argoproj_io_workflow!=""}
#             )
#           labels:
#             severity: info

